<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:statusBarColor">@android:color/transparent</item>

        <!--激活Activity中元素的过渡效果-->
        <!--        <item name="android:windowContentTransitions">true</item>-->

        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowFullscreen">false</item>
        <!--        <item name="android:windowTranslucentStatus">true</item>-->
        <!--        <item name="android:windowIsTranslucent">false</item>-->
        <item name="textAllCaps">false</item>
    </style>

    <!--Dialog透明背景-->
    <style name="NoBackgroundDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!--        <item name="android:windowBackground">@android:color/transparent</item>-->
        <!--        <item name="android:windowIsTranslucent">false</item>-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="SecondTheme" parent="AppTheme">
        <item name="android:windowEnterTransition">@transition/explode</item>
        <item name="android:windowExitTransition">@transition/explode</item>
        <!--        <item name="android:activityOpenEnterAnimation">@android:anim/slide_in_left</item>-->
        <!--        <item name="android:activityOpenExitAnimation">@anim/bottom_out</item>-->
        <!--        <item name="android:activityCloseEnterAnimation">@anim/top_in</item>-->
        <!--        <item name="android:activityCloseExitAnimation">@anim/top_out</item>-->
    </style>


</resources>
